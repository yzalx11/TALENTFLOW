git push master
  │
  │ ① 代码 + YAML 离开你电脑
  │
  ▼
┌─────────────────────────────────────────────────────────┐
│  GitHub                                               │
│  │  存储: 代码 / Dockerfile / k8s YAML                   │
│  │  版本: 每个 commit 有唯一 SHA                         │
│  └────────────────────────────────────────────────────┘
  │
  │ ② push 事件触发
  │
  ▼
┌─────────────────────────────────────────────────────────┐
│  GitHub Actions Runner (临时 Ubuntu 虚拟机)              │
│                                                         │
│  checkout    → 拉代码到这台临时机器                       │
│  docker build → 把 Python 代码 + 依赖 打包成镜像          │
│                Dockerfile 是配方，镜像是可执行文件         │
│  docker push  → 推到 ghcr.io (GitHub 的镜像仓库)          │
│                 :latest  浮动的（最新）                    │
│                 :ca95eb1 钉死的（这个 commit 的版本）       │
│  scp k8s/     → 把部署清单 YAML 传到服务器                │
│  ssh          → 远程执行部署命令                          │
│                                                         │
└─────────────────────────────────────────────────────────┘
  │
  │ ③ SCP: 传输 k8s YAML (几 KB)
  │
  ▼
┌─────────────────────────────────────────────────────────┐
│  服务器 (140.210.92.250)                                │
│                                                         │
│  Docker     ← 容器运行时，跑容器的那个人                   │
│  K3s (K8s)  ← 容器管家，管"几个副本、怎么连、挂了怎么办"    │
│                                                         │
│  kubectl apply  → K8s 读 YAML → 知道要跑什么              │
│     "我要 2 个 backend, 1 个 mysql, 1 个 redis..."       │
│                                                         │
│  kubectl set image → 钉到 :ca95eb1                       │
│     "这次跑的是这个具体版本"                               │
│                                                         │
│  K8s 做的事:                                            │
│    → 从 ghcr.io 拉镜像                                   │
│    → 创建 Pod → 分配 IP → DNS 注册                       │
│    → 健康检查 → 就绪 → 对外服务                           │
└─────────────────────────────────────────────────────────┘
  │
  │ ④ GitHub Actions 上显示 ✅
  │   你打开 http://服务器IP:21248
  │
  ▼
  系统在跑
